{
  "framework": null,
  "buildCommand": "git clone --depth 1 -b stable https://github.com/flutter/flutter.git _flutter && _flutter/bin/flutter config --enable-web && _flutter/bin/flutter pub get && _flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo skip-install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
